[tool.ruff]
line-length = 120

[tool.ruff.lint]
# Ignore specific rules that conflict with FastAPI patterns
# B008: Function call in argument defaults - required for FastAPI Depends()
# B904: Bare raise in except - sometimes needed for re-raising exceptions
ignore = [
    "B008",
    "B904",
]
