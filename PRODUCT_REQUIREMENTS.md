# Product Requirements Document — Innovo Claude

> **Audience:** AI agents and engineers making modifications to this repository.
> **Status:** Reflects current production codebase as of 2026-03.
> **Do not edit this file without re-verifying against the source code.**

---

## 1. Product Purpose

Innovo Claude is an **AI-assisted funding document generation platform** built for Innovo Consulting. It helps consultants produce *Vorhabensbeschreibungen* — formal German-language project description documents required for public funding program applications (e.g., WTT, BMBF, Eurostars).

The core problem it solves: writing funding applications requires synthesising company-specific facts, funding program rules, and a persuasive formal writing style. This process is slow, repetitive, and error-prone when done manually. The platform automates the first draft and provides an AI-assisted editing interface.

**Who uses it:** Internal Innovo consultants only. Registration is restricted to `@innovo-consulting.de` and `@aiio.de` email domains, enforced at the schema validation layer (`backend/app/schemas.py:UserCreate`).

**What it produces:** Structured multi-section Word documents (`.docx`) and PDFs in formal German funding application prose.

---

## 2. System Architecture

### 2.1 Deployment Model

The application is a single deployable unit. The FastAPI backend serves the React frontend as static files from `backend/static/`. Both processes share one origin, eliminating most CORS concerns in production.

```
User Browser
    │
    ▼
FastAPI (Uvicorn)
    ├── /api/*          ← API routes
    ├── /assets/*       ← Vite build artifacts (JS, CSS)
    └── /*              ← index.html (SPA catch-all)
```

In development, the frontend runs separately on Vite (port 5173) and the backend on port 8000. `FRONTEND_ORIGIN` controls the CORS allowlist.

### 2.2 Backend

| Concern | Technology |
|---------|-----------|
| Web framework | FastAPI 0.115 |
| ORM | SQLAlchemy 2.0 (declarative) |
| Migrations | Alembic |
| Auth | JWT HS256 via `python-jose`, bcrypt passwords |
| LLM | OpenAI Python SDK 1.x (`gpt-4o-mini`) |
| File storage | Supabase Storage (service-role key, bypasses RLS) |
| Audio transcription | OpenAI Whisper API |
| PDF generation | `reportlab` |
| DOCX generation | `python-docx` |
| PDF extraction | `PyPDF2` |
| Web scraping | `requests` + `BeautifulSoup4` |
| Analytics | PostHog 3.x |

**Entry point:** `backend/main.py`
- Loads `.env` only if the file exists (dev guard)
- Raises `RuntimeError` at startup if `JWT_SECRET_KEY` is absent
- Registers all routers under the FastAPI app
- Serves static frontend files
- Registers CORS middleware and global exception handlers

**Router layout:**

| File | Responsibility |
|------|---------------|
| `routers/auth.py` | Register, login, password reset |
| `routers/companies.py` | Company CRUD, audio upload, website crawl, background preprocessing |
| `routers/documents.py` | Document CRUD, heading confirmation, content generation, chat, export |
| `routers/funding_programs.py` | Funding program CRUD, guideline document upload |
| `routers/templates.py` | User template CRUD, system template listing |
| `routers/alte_vorhabensbeschreibung.py` | Historical document upload, style profile generation |

### 2.3 Frontend

| Concern | Technology |
|---------|-----------|
| Framework | React 19, TypeScript 5.9 |
| Build | Vite 7 |
| Routing | React Router 7 |
| Auth state | React Context API (`AuthContext`) |
| API calls | Centralised `src/utils/api.ts` |
| E2E tests | Playwright |

**Page inventory:**

| Route | Page | Purpose |
|-------|------|---------|
| `/login` | `LoginPage` | Public auth entry point |
| `/dashboard` | `DashboardPage` | Overview |
| `/companies` | `CompaniesPage` | Company management |
| `/funding-programs` | `FundingProgramsPage` | Funding program management |
| `/documents` | `DocumentsPage` | Document list |
| `/editor/:companyId/:docType` | `EditorPage` | Document editor (no nav layout) |
| `/templates` | `TemplatesPage` | Template browser |
| `/templates/new` | `TemplateEditorPage` | Create template |
| `/templates/:id/edit` | `TemplateEditorPage` | Edit template |
| `/alte-vorhabensbeschreibung` | `AlteVorhabensbeschreibungPage` | Style reference docs |

**Auth flow:**
1. POST `/auth/login` → JWT stored in `localStorage` as `innovo_auth_token`
2. `ProtectedRoute` checks `AuthContext.isAuthenticated` before rendering any protected page
3. `api.ts` injects `Authorization: Bearer <token>` on every request
4. 401 responses clear the token; each component handles `AUTH_EXPIRED`

### 2.4 Database

**Development:** SQLite (`backend/innovo.db`, auto-created)
**Production:** PostgreSQL (Supabase), connection pool size 5 + 10 overflow, `sslmode=require` auto-appended
**Schema management:** Alembic, 18+ migration versions in `backend/alembic/versions/`

### 2.5 File Storage

All uploaded files (audio, PDFs, DOCX) are stored in **Supabase Storage** using hash-based deduplication.

- **Hash:** SHA-256 of file bytes (`file_storage.py:compute_file_hash`)
- **Path format:** `{file_type}/{hash[:2]}/{hash}.{ext}` (e.g., `pdf/ab/abcd1234....pdf`)
- **Deduplication:** `get_or_create_file()` checks `files.content_hash` before uploading
- **Max upload size:** 50 MB (enforced; returns HTTP 413 if exceeded)
- **Download:** `download_from_supabase_storage()` returns raw bytes; served via API endpoints

The `files` table is the canonical record. All other tables reference it by `file_id` (UUID FK).

---

## 3. Core Workflow

### Step 1 — Company Setup

1. User creates a company with optional `website` URL and/or uploads an audio file (meeting transcript).
2. Backend dispatches a **FastAPI `BackgroundTask`** (`process_company_background` in `companies.py`).
3. Background task sequence:
   - Crawl website → `website_raw_text` → clean → `website_clean_text` (cached in `website_text_cache` by URL hash)
   - Transcribe audio via Whisper API → `transcript_raw` → clean filler words → `transcript_clean` (cached in `audio_transcript_cache` by file hash)
   - Run LLM extraction → structured `company_profile` JSON stored on `Company` row
4. `Company.processing_status` progresses: `pending` → `processing` → `done` / `failed`
5. Frontend polls `GET /companies/{id}` every 2 seconds until status is terminal.

### Step 2 — Funding Program Setup

1. User creates a funding program and optionally uploads guideline PDFs/DOCX (category `"guidelines"`).
2. On upload, text is extracted and cached in `document_text_cache` by file hash.
3. When guidelines change (combined file hash differs from stored `source_file_hash`), LLM extracts structured rules into `funding_program_guidelines_summary.rules_json`.
4. Rules cache is invalidated only when the set of guideline files changes.

### Step 3 — Style Reference Setup (Optional, System-Wide)

1. Admin uploads historical *Vorhabensbeschreibung* PDFs to the *Alte Vorhabensbeschreibung* module.
2. On "Generate style profile": combined hash of all source files → if new, LLM call → writes one row to `alte_vorhabensbeschreibung_style_profile`.
3. Only one style profile row is active at a time (keyed by `combined_hash`).
4. This profile is injected into every document generation call across the system.

### Step 4 — Document Creation

1. User navigates to the editor with query params: `?funding_program_id=X&template_id=Y&template_name=Z&title=T`
2. Backend resolves the template (`template_resolver.py`), creates a `Document` row with empty sections, and returns it.
3. Editor enters **`reviewHeadings` mode**: user can rename, add, delete, and reorder section headings.
4. User clicks "Confirm Headings" → POST `/documents/{id}/confirm-headings` → `headings_confirmed = 1`.
5. Editor enters **`confirmedHeadings` mode**: user clicks "Generate Content".

### Step 5 — Content Generation

1. POST `/documents/{id}/generate-content` triggers `_generate_batch_content()`.
2. Backend assembles context: funding rules + company profile + website/transcript text (truncated to 30 KB each) + style profile.
3. Sections are processed in batches of 3–5. Each batch is one LLM call. `milestone_table` sections are skipped.
4. Prompt order: **Rules → Company → Style → Task** (this order is load-bearing; do not change without testing).
5. Response is strict JSON: `{ section_id: "generated text", ... }`. Validated before saving. Retries up to 2× on structural failure.
6. Editor enters **`editingContent` mode**.

### Step 6 — Chat-Based Editing

1. User types an instruction in the chat panel (e.g., "Abschnitt 2.1 kürzer fassen").
2. POST `/documents/{id}/chat` → backend parses the message to identify the target section.
3. `_generate_section_content()` is called with: current section content + user instruction (XML-delimited) + company context.
4. Response is **a suggestion only** — not saved automatically. Frontend displays it with Approve / Reject buttons.
5. User clicks **Approve** → POST `/documents/{id}/chat/confirm` → section `content` updated; section `title` is never modified from suggestions.
6. User clicks **Reject** → no database write.

### Step 7 — Export

User downloads the document as DOCX (via `python-docx`) or PDF (via `reportlab`) using `GET /documents/{id}/export?format=docx|pdf`.

---

## 4. Data Models

All models are defined in `backend/app/models.py`.

### 4.1 Core Entities

#### `users`
| Column | Type | Notes |
|--------|------|-------|
| `email` | String PK | Lowercase-normalised; domain-restricted |
| `password_hash` | String | bcrypt |
| `reset_token_hash` | String (nullable) | SHA-256 of reset token |
| `reset_token_expiry` | DateTime (nullable) | 1-hour window |

#### `funding_programs`
| Column | Type | Notes |
|--------|------|-------|
| `id` | Integer PK | |
| `title` | String | |
| `website` | String (nullable) | |
| `user_email` | String FK→users | Owner |

Many-to-many with `companies` via `funding_program_companies` join table.

#### `companies`
| Column | Type | Notes |
|--------|------|-------|
| `id` | Integer PK | |
| `name` | String | |
| `website` | String (nullable) | Crawled URL |
| `website_raw_text` | Text (nullable) | Raw HTML-stripped text |
| `website_clean_text` | Text (nullable) | Navigation/boilerplate removed |
| `transcript_raw` | Text (nullable) | Raw Whisper output |
| `transcript_clean` | Text (nullable) | Filler words removed |
| `processing_status` | String | `"pending"` / `"processing"` / `"done"` / `"failed"` |
| `processing_error` | String (nullable) | Error message on failure |
| `company_profile` | JSON (nullable) | LLM-extracted structured facts |
| `extraction_status` | String (nullable) | `"pending"` / `"extracted"` / `"failed"` |
| `user_email` | String FK→users | Owner |

#### `documents`
| Column | Type | Notes |
|--------|------|-------|
| `id` | Integer PK | |
| `company_id` | Integer FK→companies | |
| `funding_program_id` | Integer FK→funding_programs (nullable) | |
| `type` | String | `"vorhabensbeschreibung"` / `"vorkalkulation"` |
| `content_json` | JSON | `{ "sections": [ { "id", "title", "content", "type"?, "milestone_data"? } ] }` |
| `chat_history` | JSON (nullable) | Array of chat message objects |
| `headings_confirmed` | Integer | `0` = unconfirmed, `1` = confirmed (Integer for SQLite compat) |
| `template_id` | UUID FK→user_templates (nullable) | If user template |
| `template_name` | String (nullable) | If system template (e.g., `"wtt_v1"`) |
| `title` | String (nullable) | Optional distinguisher |

**`content_json` section structure:**
```json
{
  "sections": [
    {
      "id": "1",
      "title": "1. Einleitung",
      "content": "...",
      "type": "text"
    },
    {
      "id": "3.2",
      "title": "3.2 Meilensteine",
      "content": "",
      "type": "milestone_table",
      "milestone_data": {
        "milestones": [...],
        "total_expenditure": 50000
      }
    }
  ]
}
```
Section IDs are strings (`"1"`, `"1.1"`, `"2.3"`). `milestone_table` sections are never sent to the LLM.

#### `files`
| Column | Type | Notes |
|--------|------|-------|
| `id` | UUID PK | |
| `content_hash` | Text (unique) | SHA-256 of file bytes |
| `file_type` | Text | `"audio"` / `"pdf"` / `"docx"` |
| `storage_path` | Text | Supabase Storage path |
| `size_bytes` | Integer | |

### 4.2 Cache Tables

| Table | Cache Key | Cached Value |
|-------|-----------|-------------|
| `audio_transcript_cache` | `file_content_hash` | Whisper transcript text |
| `website_text_cache` | `url_hash` (SHA-256 of normalised URL) | Crawled website text |
| `document_text_cache` | `file_content_hash` | Extracted PDF/DOCX text |

No TTL on any cache. Invalidation is by hash change only.

### 4.3 Derived / Computed Tables

| Table | Key | Value |
|-------|-----|-------|
| `funding_program_guidelines_summary` | `funding_program_id` (unique) | `rules_json` + `source_file_hash` (combined hash of all guideline files) |
| `alte_vorhabensbeschreibung_style_profile` | `combined_hash` (unique) | `style_summary_json` extracted writing patterns |

### 4.4 Template Tables

#### `user_templates`
| Column | Type | Notes |
|--------|------|-------|
| `id` | UUID PK | |
| `name` | String | |
| `template_structure` | JSON | Must contain `"sections"` array |
| `user_email` | String FK→users | Owner |

**System templates** are Python modules in `backend/app/templates/`. Currently only `wtt_v1` exists. Registered in `backend/app/templates/__init__.py`. Reference by `template_name = "wtt_v1"` on the Document row.

**Template resolution priority** (see `template_resolver.py:get_template_for_document`):
1. `document.template_id` → user template (ownership verified)
2. `document.template_name` → system template by name
3. Default: `"wtt_v1"`

---

## 5. LLM Pipeline

### 5.1 Model

All LLM calls use `gpt-4o-mini`. The model name is hardcoded at each call site. There is no model configuration layer or fallback.

### 5.2 Call Sites

There are **six** LLM call sites in the codebase:

| # | Function | File | Purpose | Temp | Max Tokens |
|---|----------|------|---------|------|-----------|
| 1 | `extract_company_profile()` | `extraction.py:135` | Extract structured facts from website + transcript | 0.0 | unlimited |
| 2 | `generate_style_profile()` | `style_extraction.py:95` | Extract writing style patterns from historical docs | 0.0 | 2,000 |
| 3 | `extract_rules_from_text()` | `guidelines_processing.py:110` | Extract structured rules from guideline PDFs | 0.3 | 4,000 |
| 4 | `_generate_batch_content()` | `documents.py:1203` | Generate initial section content | 0.7 | unlimited |
| 5 | `_generate_section_content()` | `documents.py:2212` | Edit existing section via chat | 0.7 | 2,000 |
| 6 | `_answer_question_with_context()` | `documents.py:2444` | Answer user questions about the document | 0.7 | 1,000 |

### 5.3 Prompt Architecture

**Extraction calls (1, 2, 3):** Deterministic (`temperature=0.0` or `0.3`). JSON output enforced via `response_format={"type": "json_object"}` where possible. Inputs are system-generated content, not user text.

**Generation call (4) — `_generate_batch_content()`:**

Prompt is assembled in four named sections, in this exact order:
```
=== 1. FÖRDERRICHTLINIEN ===      ← funding_program_rules (from guidelines_summary)
=== 2. FIRMENINFORMATIONEN ===    ← company_profile + website_clean_text[:30000] + transcript_clean[:30000]
=== 3. STIL-LEITFADEN ===         ← style_profile (from alte_vorhabensbeschreibung_style_profile)
=== 4. GENERIERUNGSAUFGABE ===    ← list of section headings to generate
```

**This order is load-bearing.** Rules are injected first so the model treats them as the primary constraint. Changing the order changes generation behaviour.

Output: strict JSON `{ "section_id": "generated text", ... }`. Validated after every attempt. Retries up to 2 additional times on structural failure (3 total API calls worst-case).

**Edit call (5) — `_generate_section_content()`:**

The system message explicitly labels the model as an *editor*, not an *author*. Prompt structure:
```
{style_guide}
SIE SIND EIN REDAKTEUR, KEIN AUTOR.     ← role constraint
Aktueller Inhalt: {current_content}      ← existing text (must exist)
Benutzeranweisung: <user_instruction>    ← user instruction, XML-delimited
{instruction_text}
</user_instruction>
Firmeninformationen (NUR ZUR STÜTZUNG):  ← company context (support only)
{company_context}
```

Output: plain German text (no JSON). Markdown artifacts are stripped via regex post-processing.

**Q&A call (6) — `_answer_question_with_context()`:**

Prompt structure:
```
KONTEXT:
{document_content}        ← all section text concatenated
{website_summary}         ← company website excerpt
{conversation_history}    ← last 3 chat messages

Firmenname: {company_name}
BENUTZERFRAGE: <user_instruction>   ← user question, XML-delimited
{user_query_text}
</user_instruction>
```

### 5.4 Prompt Injection Protection

User-controlled inputs (`instruction`, `user_query`) are wrapped in XML delimiters before injection into all prompts:

```xml
<user_instruction>
{user_input}
</user_instruction>
```

None guards are applied before wrapping: `instruction_text = instruction or ""`.

### 5.5 Token Logging

Before every LLM call, prompt size is logged:

```python
approx_tokens = len(prompt) // 4
logger.info("LLM {step} prompt size (chars): %s", len(prompt))
logger.info("LLM {step} prompt tokens: %s", approx_tokens)
```

Step labels: `batch generation`, `section edit`, `Q&A`, `company profile extraction`, `guideline extraction`, `style extraction`.

### 5.6 Input Truncation

| Input | Limit | Applied in |
|-------|-------|-----------|
| Company extraction (website + transcript combined) | 50,000 chars each | `extraction.py:78-79` (smart truncate: 60% head + 40% tail) |
| Style extraction (all docs combined) | 100,000 chars | `style_extraction.py:57-59` (head truncation) |
| Guideline extraction | 50,000 chars | `guidelines_processing.py:83` (inline slice in f-string) |
| Website text in generation prompt | 30,000 chars | `documents.py:~990` |
| Transcript text in generation prompt | 30,000 chars | `documents.py:~999` |

### 5.7 Caching Strategy

LLM calls are avoided by hash-based caching at two levels:

1. **Text extraction caches** (`audio_transcript_cache`, `website_text_cache`, `document_text_cache`): keyed on file content hash or URL hash. Same file uploaded twice → one Whisper call.
2. **Derived output caches** (`funding_program_guidelines_summary`, `alte_vorhabensbeschreibung_style_profile`): keyed on combined hash of all source files. Re-upload of the same files → no new LLM call.

---

## 6. Engineering Constraints

These constraints are hard rules for all modifications to this codebase.

### 6.1 From `CLAUDE.md` (mandatory)

1. **Never perform large refactors without explicit user approval.** Prefer small, incremental changes.
2. **Do not modify database schema unless explicitly requested.** Do not run migrations automatically.
3. **Do not modify environment variables.**
4. **Do not modify deployment scripts** (Render, Docker) unless instructed.
5. **Always follow the change workflow:** explain → propose diff → wait for approval → apply.
6. **Preserve API contracts.** Do not change response shapes or break existing endpoints.
7. **Avoid injecting unnecessary context into prompts.** Do not increase temperature or token limits without justification.
8. **Never log sensitive company data.** No prompt content, website text, or section text in log lines.

### 6.2 Database Constraints

- **Primary key types are mixed:** `users.email` is a String PK (not an integer). All user ownership checks use `user_email` string comparisons.
- **`headings_confirmed` is Integer (0/1)**, not Boolean, for SQLite compatibility. PostgreSQL also accepts this. Do not change the column type.
- **`content_json` is an unvalidated JSON blob.** Any code reading sections must defensively check for required keys (`id`, `title`, `content`). Missing keys have caused silent failures.
- **`chat_history` column was added in a late migration.** The `_safe_get_document_by_id()` function in `documents.py` has a three-strategy fallback to handle databases where the column may not exist. Do not remove this until the migration is verified in all environments.
- **No cascade deletes** are configured. Deleting a company does not delete its documents.

### 6.3 LLM Constraints

- **Do not change prompt wording without testing generation quality.** Prompts are in German. Even minor rewording can shift output tone.
- **Do not remove the `<user_instruction>` XML delimiters** from `instruction` or `user_query` injections.
- **Do not raise `max_tokens` limits** without justification. Current limits were set to bound cost.
- **Do not lower `temperature` below 0.7 for generation calls** (calls 4 and 5). These need creative variation; deterministic generation produces repetitive documents.
- **`milestone_table` sections must never be sent to the LLM.** The batch generator explicitly skips them at `documents.py:1068`. Do not remove this guard.
- **Section titles must never be updated from LLM suggestions.** The `/chat/confirm` endpoint updates `content` only. This is intentional — headings are confirmed by the user and treated as fixed structure.

### 6.4 Frontend Constraints

- **All API calls must go through `src/utils/api.ts`.** Do not hardcode API URLs or create parallel fetch logic.
- **Do not add new global state** (new Contexts, global stores) without explicit approval.
- **`ProtectedRoute` must wrap all authenticated pages.** Do not bypass it.
- **Handle `AUTH_EXPIRED` explicitly** in components that call the API — it is not auto-redirected centrally.
- **File uploads must preserve the multipart/FormData contract.** Do not set `Content-Type` manually on upload requests — the browser sets the boundary automatically.

### 6.5 Security Constraints

- **Never make unrestricted external HTTP requests.** Website scraping in `website_scraping.py` has a known SSRF risk (no private IP blocklist). Any new external request code must validate the target URL against RFC 1918 ranges before connecting.
- **Never log prompt content, section text, company data, or user queries.** Log only metadata (IDs, lengths, status codes).
- **JWT secret must come from environment.** `JWT_SECRET_KEY` raises `RuntimeError` at startup if absent. Do not provide a default value.
- **Password reset tokens must not appear in API responses** in production. The dev-only token leak in `auth.py` must be removed before any production deployment.

### 6.6 Environment Variables

| Variable | Required | Default | Purpose |
|----------|----------|---------|---------|
| `JWT_SECRET_KEY` | Yes | — | JWT signing secret (raises RuntimeError if absent) |
| `OPENAI_API_KEY` | Yes (functional) | — | OpenAI API calls (logs warning if absent; features fail) |
| `DATABASE_URL` | No | `sqlite:///./innovo.db` | Database connection string |
| `FRONTEND_ORIGIN` | No | localhost whitelist | CORS allowed origin |
| `SUPABASE_URL` | No | — | Supabase project URL |
| `SUPABASE_KEY` | No | — | Supabase service-role key |
| `SUPABASE_STORAGE_BUCKET` | No | `"files"` | Storage bucket name |
| `UPLOAD_DIR` | No | `backend/uploads/audio` | Local audio upload directory |
| `POSTHOG_API_KEY` | No | — | Analytics (silently disabled if absent) |
| `POSTHOG_HOST` | No | `https://us.i.posthog.com` | PostHog ingest URL |
| `POSTHOG_DISABLED` | No | `"false"` | Explicit disable flag |
| `DEBUG_ENV_LOG` | No | — | Enables verbose env var logging on startup |
| `VITE_API_URL` | No (frontend) | `http://localhost:8000` | Backend base URL for frontend |

### 6.7 Known Technical Debt

The following issues exist in the codebase and should not be accidentally made worse:

1. **`documents.py` is 3,300+ lines.** It contains document CRUD, company context formatting, prompt construction, batch generation, chat orchestration, section parsing (with 8 regex fallback strategies), Q&A, milestone extraction, and export. Future work should extract these into separate modules.
2. **No rate limiting on any endpoint.** Auth endpoints (login, password reset) and LLM endpoints are unrestricted per user.
3. **2-second frontend polling** on company processing status creates N × 0.5 read/s per concurrent user. There is no backoff and no push mechanism.
4. **SSRF in `website_scraping.py`.** User-supplied URLs are fetched without validating against private IP ranges. `allow_redirects=True` compounds this.
5. **Password reset token returned in API response** (dev-only code path in `auth.py`). Must be removed before production use.
6. **`content_json` is an unvalidated blob.** No Pydantic schema enforces the section structure at the API boundary.

---

## 7. File Map for Common Tasks

| Task | Files to read | Files to modify |
|------|--------------|----------------|
| Change generation prompt | `documents.py` (`_generate_batch_content`) | `documents.py` |
| Change edit prompt | `documents.py` (`_generate_section_content`) | `documents.py` |
| Add a system template | `templates/__init__.py`, `templates/wtt_v1.py` | New file in `templates/`, update `__init__.py` |
| Add a new document section type | `models.py` (content_json docs), `documents.py` | `documents.py`, `EditorPage.tsx` |
| Change auth token expiry | `jwt_utils.py` | `jwt_utils.py` |
| Change company processing steps | `routers/companies.py`, `preprocessing.py`, `extraction.py` | Relevant module |
| Change guideline extraction rules | `guidelines_processing.py` | `guidelines_processing.py` |
| Change style extraction | `style_extraction.py` | `style_extraction.py` |
| Change file upload behaviour | `file_storage.py`, relevant router | As needed |
| Add a new API endpoint | Router file, `schemas.py`, `models.py` (if schema change needed) | As needed |
| Change frontend auth flow | `AuthContext.tsx`, `ProtectedRoute.tsx`, `api.ts` | As needed |
| Change editor modes or UX | `EditorPage.tsx` | `EditorPage.tsx` |

---

*This document was generated by analysing the repository source code directly. It reflects implementation-level reality, not aspirational design.*
